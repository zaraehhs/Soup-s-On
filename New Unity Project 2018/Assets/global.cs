using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class global : MonoBehaviour
{
    public static int recipeChoice = 1;
    // Start is called before the first frame update
    void Start()
    {
        //recipeNumber = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
